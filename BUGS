Cannot set player's wield index, can only get it.  Sometimes switching to/from
node class results in bugs.  Like getting an item in your hand from prior
class.  Or the hotbar on the hud showing visual glitches (vertical lines).
